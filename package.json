{
  "scripts": {
    "prettier": "prettier -c .",
    "prettier:fix": "prettier -w .",
    "emulators": "npm --prefix functions run build && npm --prefix client run build:dev && firebase emulators:start --project stage",
    "emulators:preview": "npm --prefix functions run build && npm --prefix client run build:test && firebase emulators:start --project stage"
  },
  "devDependencies": {
    "prettier": "^2.7.1"
  },
  "dependencies": {
    "@firebase/functions-types": "^0.5.1"
  }
}
